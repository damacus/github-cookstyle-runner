#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative '../lib/cookstyle_runner/cli'

begin
  cli = CookstyleRunner::CLI.new(ARGV)
  exit_code = cli.run
  exit exit_code
rescue StandardError => e
  warn "Error: #{e.message}"
  warn e.backtrace.join("\n") if ENV['DEBUG']
  exit 1
end
